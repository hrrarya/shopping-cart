import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  appbar: {
    display: "flex",
    justifyContent: "space-between",
    flexDirection: "row",
  },
});

export { useStyles };
